namespace TrimDB.Nethermind.Interfaces
{
    public enum TuneType
    {
        Default,
        WriteBias,
        ReadBias,
        HeavyWrite,
        AggressiveCompaction,
        EnableBlobFiles,
        DisableCompaction,
        HashDb,
    }
}
